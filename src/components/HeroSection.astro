---
// src/components/HeroSection.astro
---
<section id="inicio" class="pt-20 lg:pt-0 min-h-screen flex items-center pb-12 relative overflow-hidden transition-colors duration-500 bg-[#0a0a0a]">
    
    <div class="absolute inset-0 z-0 pointer-events-none">
        <img 
            src="https://abhimanyud3dx.github.io/superqbit-static-resources/resources/images/bg/bg1.png" 
            alt="Background Grid" 
            class="w-full h-full object-cover dark:opacity-45 "
        />
    </div>

    <div class="hidden lg:block absolute inset-0 z-[2] pointer-events-none">
        <div class="avioneta-container absolute top-1/3 left-[-100px]">
            <div class="flex flex-col items-center">
                <svg class="w-10 h-10 text-blue-400/40 -rotate-12" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M21,16V14L13,9V3.5A1.5,1.5 0 0,0 11.5,2A1.5,1.5 0 0,0 10,3.5V9L2,14V16L10,13.5V19L8,20.5V22L11.5,21L15,22V20.5L13,19V13.5L21,16Z" />
                </svg>
                <div class="w-20 h-[1px] bg-gradient-to-l from-blue-400/10 to-transparent mt-1 mr-8"></div>
            </div>
        </div>
    </div>

    <div class="max-w-7xl mx-auto px-4 w-full grid lg:grid-cols-2 gap-16 items-center">
        <div class="space-y-8 animate-fade-in-up z-10 order-2 lg:order-1">
            <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-400 font-bold tracking-wider uppercase transition-colors">
                <span class="w-2 h-2 rounded-full bg-blue-600 dark:bg-blue-400 animate-pulse"></span>
                Open to Work
            </div>
            
            <h1 class="text-5xl lg:text-7xl font-bold text-gray-900 dark:text-white leading-[1.1] transition-colors">
                Architecting Scalable <br/>
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-indigo-600">
                    Cloud Solutions
                </span>
            </h1>
            
            <p class="text-lg text-gray-600 dark:text-gray-400 max-w-lg leading-relaxed border-l-4 border-blue-200 dark:border-blue-700 pl-4 transition-colors">
                Transformando negocios a través de la arquitectura Salesforce de alto rendimiento.
            </p>

            <div class="flex flex-wrap gap-4 pt-2">
                <a href="#contact" class="px-8 py-3.5 bg-blue-600 text-white rounded-xl font-semibold hover:bg-blue-700 hover:shadow-lg hover:shadow-blue-500/30 transition-all transform hover:-translate-y-1">
                    Contactar
                </a>
                <a href="#stack" class="px-8 py-3.5 bg-white dark:bg-gray-700 text-gray-700 dark:text-white border border-gray-200 dark:border-gray-600 rounded-xl font-semibold hover:bg-gray-50 dark:hover:bg-gray-600 hover:border-blue-300 transition-all">
                    Ver Stack
                </a>
            </div>
        </div>

        <div class="relative order-1 lg:order-2 flex justify-center lg:justify-end animate-fade-in-delayed z-10 ">
            <div class="relative w-[350px] md:w-[450px] aspect-[4/5] min-w-64"> 
                
                <img src="https://images.unsplash.com/photo-1556157382-97eda2d62296?auto=format&fit=crop&q=80&w=800" 
                    alt="Profile" 
                    class="w-full h-full object-cover rounded-[2rem] shadow-2xl z-10 relative transition-opacity duration-700">
                
                <div class="absolute -top-6 -left-8 z-20 bg-white/90 dark:bg-gray-700/90 backdrop-blur-md p-4 rounded-2xl shadow-[0_20px_50px_rgba(0,0,0,0.1)] dark:shadow-none border border-white/50 dark:border-gray-600/50 animate-float-slow max-w-[160px] transition-colors duration-500">
                    <div class="flex items-center gap-3">
                        <div class="p-2 bg-blue-100 dark:bg-blue-900/50 rounded-lg text-blue-600">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        </div>
                        <div class="text-gray-900 dark:text-white">
                            <p class="text-2xl font-bold leading-none">+4</p>
                            <p class="text-xs text-gray-500 dark:text-gray-400 font-medium uppercase mt-1">Años de Experiencia</p>
                        </div>
                    </div>
                </div>

                <div class="absolute -bottom-8 -right-4 z-30 bg-gray-900 dark:bg-gray-700 text-white p-5 rounded-2xl shadow-2xl animate-float-medium max-w-[180px]">
                    <p class="text-3xl font-bold text-blue-400 mb-1">13</p>
                    <p class="text-sm text-gray-300 leading-tight">Certificaciones Oficiales Salesforce</p>
                </div>

                <div class="absolute top-12 -right-10 z-20 bg-white dark:bg-gray-700 p-4 rounded-xl shadow-xl animate-float-fast animate-float-medium max-w-[180px] border border-gray-100 dark:border-gray-600 hidden md:block transform rotate-3 transition-colors duration-500">
                    <div class="text-center">
                        <p class="text-2xl font-bold text-indigo-600">+30</p>
                        <p class="text-[10px] text-gray-400 dark:text-gray-300 uppercase font-bold tracking-wider">Proyectos</p>
                    </div>
                </div>

                <div class="absolute -bottom-10 -left-10 w-40 h-40 bg-blue-600/10 rounded-full blur-2xl -z-10"></div>
                <div class="absolute top-10 -right-10 w-40 h-40 bg-indigo-600/10 rounded-full blur-2xl -z-10"></div>
            </div>
        </div>
    </div>
</section>

<style>
    /* Animación de la Avioneta */
    .avioneta-container {
        animation: fly-across 30s linear infinite;
    }

    @keyframes fly-across {
        0% { transform: translate(-10vw, 0) rotate(5deg); opacity: 0; }
        10% { opacity: 1; }
        90% { opacity: 1; }
        100% { transform: translate(110vw, -150px) rotate(-5deg); opacity: 0; }
    }

    /* Animaciones de los badges */
    @keyframes float-slow { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-15px); } }
    @keyframes float-medium { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-20px); } }
    .animate-float-slow { animation: float-slow 5s ease-in-out infinite; }
    .animate-float-medium { animation: float-medium 4s ease-in-out infinite; }

    .animate-fade-in-up { animation: fadeInUp 0.8s ease-out forwards; }
    .animate-fade-in-delayed { animation: fadeIn 1.2s ease-out forwards; opacity: 0; }

    @keyframes fadeInUp {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
</style>